/*function calcAge (birthday) {
    birthday = new Date(birthday);
    today     = new Date();
 
    var years = (today.getFullYear() - birthday.getFullYear());
 
    if (today.getMonth() < birthday.getMonth() || 
        today.getMonth() == birthday.getMonth() && today.getDate() < birthday.getDate()) {
        years--;
    }
 
    return years;
}

function data(annoNascita) {
    var d = new Date();
    var lAnno = d.getFullYear();
    var eta = lAnno - annoNascita;
    var mancaA100 = 100 - eta;
    document.write("<p>la tua età:" + eta + "<p/>");
    document.write("<p>a 100 anni mancano:" + mancaA100 + "<p/>");
}*/

